[project]
name = "email-triage-env"
version = "1.0.0"
description = "OpenEnv-compliant email triage RL environment"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.27.0",
    "pydantic>=2.6.0",
    "requests>=2.31.0",
    "openai>=1.12.0",
]
